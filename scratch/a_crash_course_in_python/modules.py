import re
my_regex = re.compile(r"[0-9]+", re.I)

import re as regex
my_regex = regex.compile(r"[0-9]+", regex.I)

import matplotlib.pyplot as plt

from collections import Counter, defaultdict
lookup = defaultdict(int)
my_counter = Counter()